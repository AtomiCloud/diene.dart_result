# ### dart-lib-cd
# #### source: dart-lib
name: CD

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  publish:
    name: Publish to pub.dev
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/⚡reusable-publish.yaml
    secrets: inherit
