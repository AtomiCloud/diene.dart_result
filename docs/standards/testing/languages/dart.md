# Dart testing variant

Use `flutter_test` for unit and widget coverage. Inject clocks and gateways, use `SharedPreferences.setMockInitialValues`, and assert user-visible async, locale, theme, error, and narrow-layout behavior. Visual screenshots are review inputs, never golden gates.
