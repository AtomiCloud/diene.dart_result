# Dart validation variant

Validate configuration and transport at boundaries, domain invariants in constructors, and form input live in `FormField` validators. Return structured Problems for expected failures and keep server-side validation authoritative.
