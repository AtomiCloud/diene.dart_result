# Dart functional practices variant

Prefer immutable values, pure mapping functions, exhaustive switches on sealed classes, and `Result<T>` at expected failure boundaries. Keep side effects in injected gateways and controllers; never hide discarded futures.
