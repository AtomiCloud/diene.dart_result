# Dart date and time variant

Use typed `LocalDate`, `LocalTime`, UTC instant, ISO duration, and IANA timezone values at domain boundaries. Serialize only canonical C0 strings. Do not use a local `DateTime` where the domain means a calendar date or wall-clock time.
