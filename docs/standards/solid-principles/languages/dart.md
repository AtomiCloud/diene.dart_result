# Dart SOLID variant

Use narrow abstract interfaces for gateways, constructor injection, and focused controllers. Depend on domain-facing contracts rather than Firebase, Logto, HTTP, or Flutter implementations. Extend behavior through new adapters instead of branching on concrete types.
