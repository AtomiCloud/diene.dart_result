# Dart authorization variant

Model authentication and authorization separately. Keep claims in typed domain values, inject policy evaluators, and return typed `Result`/`Problem` failures. Flutter route guards preserve `returnTo`; widgets never decide policy from raw token strings.
